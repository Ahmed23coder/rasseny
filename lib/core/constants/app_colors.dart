import 'dart:ui';

/// Centralized color constants matching the Rasseny Obsidian design system.
abstract final class AppColors {
  // ── Primary Background ──
  static const Color midnightNavy = Color(0xFF001428);
  static const Color scaffoldDark = Color(0xFF121315);

  // ── Radial Gradient Stops ──
  static const Color gradientCenter = Color(0xFF1E3650);
  static const Color gradientMiddle = Color(0xFF0F253C);
  static const Color gradientEdge = Color(0xFF001428);

  // ── Accent & Text ──
  static const Color silver = Color(0xFFC6C6C6);
  static const Color subtitleBlue = Color(0xFF9AB8D8);

  // ── Onboarding ──
  static const Color headlineBlue = Color(0xFFD1E4FF);
  static const Color descriptionGrey = Color(0xFFC3C6CE);
  static const Color accentBlue = Color(0xFFACCAEA);
  static const Color buttonDark = Color(0xFF2F3131);
  static const Color ghostBorder = Color(0xFF43474D);
  static const Color iceWhite = Color(0xFFF0F4F8);

  // ── Auth – Cards & Surfaces ──
  static const Color cardDark = Color(0xFF1B1C1E);
  static const Color inputFill = Color(0xFF343537);
  static const Color inputFillDark = Color(0xFF292A2C);

  // ── Auth – Text ──
  static const Color textPrimary = Color(0xFFE3E2E4);
  static const Color textSecondary = Color(0xFFC6C6C6);
  static const Color labelBlue = Color(0xFFB0C9E8);

  // ── Auth – Buttons ──
  static const Color silverGold = Color(0xFFC0C0C0);
  static const Color navyButton = Color(0xFF102A43);

  // ── Auth – Error ──
  static const Color error = Color(0xFFEF4444);
  static const Color errorText = Color(0xFFFFB4AB);

  // ── Auth – Accent ──
  static const Color accentGold = Color(0xFFE8C08C);
  static const Color accentGoldDark = Color(0xFF442B04);

  // ── Auth – Borders ──
  static const Color inputBorderBlue = Color(0xFF486581);
  static const Color subtleBorder = Color(0xFF6B7280);

  // ── Auth – Misc ──
  static const Color hintText = Color(0xFF8D9198);
}
